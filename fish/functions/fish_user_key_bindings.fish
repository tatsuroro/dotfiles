function fish_user_key_bindings
  bind \cs peco_select_ghq_repository
  bind \cr peco_select_history
  bind \cz peco_recentd
  bind \cq peco_kill
  bind \cx\ct peco_todoist_item
  bind \cx\cp peco_todoist_project
  bind \cx\cl peco_todoist_labels
  bind \cx\cc peco_todoist_close
  bind \cx\cx peco_todoist_delete
end
