# ~/.tmuxinator/fam.yml

name: cabuki-facebook
root: ~/workspace/cabuki/cabuki-facebook

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: rbenv shell 2.1.2

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

windows:
  - local:
      layout: bb17,364x88,0,0[365x62,0,0{247x62,0,0,0,117x62,248,0,2},365x26,0,63{182x26,0,63,3,182x26,183,63,4}]
      panes:
        - gst
        -
        - tail -f logs/application.log
        - 
  - remote:
      layout: 1d4c,364x88,0,0[365x47,0,0{101x47,0,0,1,135x47,102,0,8,127x47,238,0,5},365x41,0,48,7]
      panes:
        - Dev:
            - 
        - Dev2:
            - 
        - Grunt:
            - 
        - Log:
            - 
